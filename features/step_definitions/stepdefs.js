const assert = require('assert');
const { Given, When, Then } = require('@cucumber/cucumber');


Given('Main menu', (index.html)=>{
    visit(index.html)
    });

    When('User click Iniciar Partida', ()=>{

    })

    Then('Redirect keyboard view'()=>{
        
    })